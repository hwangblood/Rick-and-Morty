package com.hwangblood.rickandmorty.epoxy

import com.hwangblood.rickandmorty.R
import com.hwangblood.rickandmorty.databinding.ModelLoadingBinding

class LoadingEpoxyModel : EpoxyViewBindingModel<ModelLoadingBinding>(R.layout.model_loading) {
    override fun ModelLoadingBinding.bind() {
        // nothing to do
    }
}